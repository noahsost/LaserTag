public class Player {
    int id, score;
    String firstName, lastName, codeName;
    Player(int idNum, String first, String last, String code){
        id = idNum;
        firstName = first;
        lastName = last;
        codeName = code;
    }
}
